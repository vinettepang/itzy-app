# 与 apps/server/.env 中的 DATABASE_URL 应指向同一库，便于 prisma db push / generate。
#
# P1000 = 认证失败：把 USER、PASSWORD 改成你本机 MySQL 真实账号（示例 password 仅为占位）。
# 空密码示例: mysql://root:@127.0.0.1:3306/itzy_app
# 自测: mysql -h127.0.0.1 -uroot -p itzy_app
DATABASE_URL="mysql://root:password@127.0.0.1:3306/itzy_app"
