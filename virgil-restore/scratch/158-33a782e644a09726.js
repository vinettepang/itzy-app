"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[158],{634:(t,e,n)=>{n.d(e,{J:()=>v,B:()=>h});var o=n(5155),i=n(2115),a=n(63),r=n(308),l=n(1247),u=n(1762),s=n(5081),c=n(5310);function m(){return"undefined"!=typeof document&&document.documentElement.classList.contains("site-inverted")}function d(){let t=(0,a.usePathname)(),{advanceTheme:e}=h(),n="/aj1"===t||t.startsWith("/aj1/")||"/usmnt"===t||t.startsWith("/usmnt/"),o="/usmnt"===t||t.startsWith("/usmnt/");return(0,i.useEffect)(()=>()=>{document.documentElement.classList.remove("site-inverted")},[]),(0,i.useEffect)(()=>{let t=t=>{if(!function(){if("undefined"==typeof document)return!1;let t=document.activeElement;if(!t)return!1;let e=t.tagName.toLowerCase();return"input"===e||"textarea"===e||t.isContentEditable}())if("i"===t.key||"I"===t.key){t.preventDefault();let e=function(){var t;let e=!m();return t=e,"undefined"!=typeof document&&m()!==t&&(document.documentElement.classList.toggle("site-inverted",t),window.dispatchEvent(new Event("usmnt-site-invert-change"))),e}();o&&(0,c.o)()&&(0,s.wy)(e)}else if("y"===t.key||"Y"===t.key){t.preventDefault();let e=!(0,s.MT)();if((0,s.wy)(e),e&&o&&!(0,c.o)()){let{clientX:t,clientY:e}=(0,u.J3)();(0,u.gX)(t,e)}}else"Space"!==t.code||n||(t.preventDefault(),e())};return window.addEventListener("keydown",t),()=>window.removeEventListener("keydown",t)},[e,o,n]),null}let f=(0,i.createContext)(null);function v(t){let{children:e}=t,n=(0,a.usePathname)(),[u,s]=(0,i.useState)(0),c="/aj1"===n||n.startsWith("/aj1/")||"/usmnt"===n||n.startsWith("/usmnt/"),m=c?(0,l.bA)():(0,l.TD)(u),v=(0,i.useCallback)(()=>{s(t=>(t+1)%l.J6)},[]);return(0,i.useLayoutEffect)(()=>{var t;c?document.documentElement.setAttribute("data-nike-theme",""):document.documentElement.removeAttribute("data-nike-theme"),document.documentElement.style.backgroundColor=m.colors.background,document.body.style.backgroundColor=m.colors.background,document.body.style.color=m.colors.text,document.body.style.transition="background-color ".concat(l.LT,", color ").concat(l.LT),document.documentElement.style.transition="background-color ".concat(l.LT);let e=document.querySelectorAll('meta[name="theme-color"]');for(let t=1;t<e.length;t++)e[t].remove();let n=null!=(t=e[0])?t:document.querySelector('meta[name="theme-color"]');n||((n=document.createElement("meta")).setAttribute("name","theme-color"),document.head.appendChild(n)),n.setAttribute("content",m.colors.background)},[c,m]),(0,o.jsx)(f.Provider,{value:{themeIndex:u,theme:m,advanceTheme:v},children:(0,o.jsx)(r.NP,{theme:m,children:(0,o.jsxs)("div",{style:{"--selection-bg":m.colors.text,"--selection-color":m.colors.background},children:[e,(0,o.jsx)(d,{})]})})})}function h(){let t=(0,i.useContext)(f);if(!t)throw Error("useThemeCycle must be used within ThemeCycleProvider");return t}},789:(t,e,n)=>{n.d(e,{i:()=>o});function o(t){if(!(t instanceof Element))return!1;let e=t.closest("a");return null!=e&&null!=e.closest("header")}},1203:(t,e,n)=>{n.d(e,{_t:()=>o,hI:()=>r,od:()=>i,tR:()=>a});let o="\n  uniform float uCurlAmount;\n  uniform float uCurlTightness;\n  uniform float uCurlOrigin;\n  uniform float uCurlOriginEdge;\n  uniform float uAspect;\n\n  vec3 applyCurl(vec2 p) {\n    float width = uAspect;\n    float height = 1.0;\n    float PI = 3.14159265359;\n    float apexX = (uCurlOriginEdge > 0.5) ? (width * 0.5) : (-width * 0.5);\n    float apexY = mix(height * 0.5, -height * 0.5, uCurlOrigin);\n    float t = uCurlOrigin;\n    float amountAbs = abs(uCurlAmount);\n    float thetaFlat = PI * 0.5;\n    float thetaTight = 0.05 + 0.2 * (1.0 - uCurlTightness);\n    float theta = mix(thetaFlat, thetaTight, amountAbs);\n    float sinTheta = max(sin(theta), 0.001);\n    float viX_t = apexX - p.x;\n    float viY_t = apexY - p.y;\n    float R_t = max(sqrt(viX_t * viX_t + viY_t * viY_t), 0.0001);\n    float r_t = R_t * sin(theta);\n    float beta_t = asin(clamp(viX_t / R_t, -1.0, 1.0)) / sinTheta;\n    vec3 curledTop = vec3(apexX - r_t * sin(beta_t), apexY - (R_t - r_t * (1.0 - cos(beta_t)) * sin(theta)), (uCurlAmount >= 0.0) ? -r_t * (1.0 - cos(beta_t)) * cos(theta) : r_t * (1.0 - cos(beta_t)) * cos(theta));\n    float viX_b = p.x - apexX;\n    float viY_b = p.y - apexY;\n    float R_b = max(sqrt(viX_b * viX_b + viY_b * viY_b), 0.0001);\n    float r_b = R_b * sin(theta);\n    float beta_b = asin(clamp(viX_b / R_b, -1.0, 1.0)) / sinTheta;\n    vec3 curledBot = vec3(apexX + r_b * sin(beta_b), apexY + (R_b - r_b * (1.0 - cos(beta_b)) * sin(theta)), (uCurlAmount >= 0.0) ? -r_b * (1.0 - cos(beta_b)) * cos(theta) : r_b * (1.0 - cos(beta_b)) * cos(theta));\n    vec3 curled = mix(curledTop, curledBot, t);\n    float R = mix(R_t, R_b, t);\n    float maxDist = sqrt(width * width + height * height);\n    float distFactor = smoothstep(0.0, maxDist * 0.6, R);\n    float amountAtVertex = amountAbs * (0.5 + 0.5 * distFactor);\n    return mix(vec3(p, 0.0), curled, min(1.0, amountAtVertex));\n  }\n\n  varying vec2 vUv;\n\n  void main() {\n    vUv = uv;\n    vec2 p = position.xy;\n    vec3 pos = applyCurl(p);\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);\n  }\n",i="\n  uniform sampler2D map;\n  varying vec2 vUv;\n\n  void main() {\n    vec4 texColor = texture2D(map, vUv);\n    if (texColor.a < 0.01) discard;\n    gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);\n  }\n",a="\n  uniform float uCurlAmount;\n  uniform float uCurlTightness;\n  uniform float uCurlOrigin;\n  uniform float uCurlOriginEdge;\n  uniform float uAspect;\n\n  varying vec2 vUv;\n  varying vec3 vWorldPosition;\n  varying vec3 vWorldNormal;\n\n  vec3 applyCurl(vec2 p) {\n    float width = uAspect;\n    float height = 1.0;\n    float PI = 3.14159265359;\n\n    float apexX = (uCurlOriginEdge > 0.5) ? (width * 0.5) : (-width * 0.5);\n    float apexY = mix(height * 0.5, -height * 0.5, uCurlOrigin);\n\n    float t = uCurlOrigin;\n    float amountAbs = abs(uCurlAmount);\n    float thetaFlat = PI * 0.5;\n    float thetaTight = 0.05 + 0.2 * (1.0 - uCurlTightness);\n    float theta = mix(thetaFlat, thetaTight, amountAbs);\n    float sinTheta = max(sin(theta), 0.001);\n\n    float viX_t = apexX - p.x;\n    float viY_t = apexY - p.y;\n    float R_t = max(sqrt(viX_t * viX_t + viY_t * viY_t), 0.0001);\n    float r_t = R_t * sin(theta);\n    float beta_t = asin(clamp(viX_t / R_t, -1.0, 1.0)) / sinTheta;\n    vec3 curledTop = vec3(apexX - r_t * sin(beta_t), apexY - (R_t - r_t * (1.0 - cos(beta_t)) * sin(theta)), (uCurlAmount >= 0.0) ? -r_t * (1.0 - cos(beta_t)) * cos(theta) : r_t * (1.0 - cos(beta_t)) * cos(theta));\n\n    float viX_b = p.x - apexX;\n    float viY_b = p.y - apexY;\n    float R_b = max(sqrt(viX_b * viX_b + viY_b * viY_b), 0.0001);\n    float r_b = R_b * sin(theta);\n    float beta_b = asin(clamp(viX_b / R_b, -1.0, 1.0)) / sinTheta;\n    vec3 curledBot = vec3(apexX + r_b * sin(beta_b), apexY + (R_b - r_b * (1.0 - cos(beta_b)) * sin(theta)), (uCurlAmount >= 0.0) ? -r_b * (1.0 - cos(beta_b)) * cos(theta) : r_b * (1.0 - cos(beta_b)) * cos(theta));\n\n    vec3 curled = mix(curledTop, curledBot, t);\n    // Amplify curl by distance: far vertices curl more, near vertices still curl\n    float R = mix(R_t, R_b, t);\n    float maxDist = sqrt(width * width + height * height);\n    float distFactor = smoothstep(0.0, maxDist * 0.6, R);\n    float amountAtVertex = amountAbs * (0.5 + 0.5 * distFactor);\n    return mix(vec3(p, 0.0), curled, min(1.0, amountAtVertex));\n  }\n\n  void main() {\n    vUv = uv;\n    vec2 p = position.xy;\n    vec3 pos = applyCurl(p);\n\n    // Compute surface normal from curl deformation via finite differences.\n    // Use boundary-aware sampling so we never sample outside the mesh\n    // (top row at y=0.5 would otherwise sample y+eps>0.5 -> wrong normals).\n    float eps = 0.001;\n    float xMax = uAspect * 0.5;\n    float yMax = 0.5;\n    vec3 tangentX = (p.x + eps > xMax)\n      ? (pos - applyCurl(p - vec2(eps, 0.0)))\n      : (applyCurl(p + vec2(eps, 0.0)) - pos);\n    vec3 tangentY = (p.y + eps > yMax)\n      ? (pos - applyCurl(p - vec2(0.0, eps)))\n      : (applyCurl(p + vec2(0.0, eps)) - pos);\n    vec3 curlNormal = normalize(cross(tangentX, tangentY));\n    if (dot(curlNormal, vec3(0.0, 0.0, 1.0)) < 0.0) curlNormal = -curlNormal;\n\n    vec4 worldPos = modelMatrix * vec4(pos, 1.0);\n    vWorldPosition = worldPos.xyz;\n    vWorldNormal = normalize((modelMatrix * vec4(curlNormal, 0.0)).xyz);\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);\n  }\n",r="\n  uniform sampler2D map;\n  uniform sampler2D uFoilMap;\n  uniform float matteRoughness;\n  uniform float reflectiveStrength;\n  uniform vec3 uCameraPosition;\n  uniform vec3 uLight1Position;\n  uniform float uLight1Intensity;\n  uniform vec3 uLight2Position;\n  uniform float uLight2Intensity;\n  uniform float uTime;\n  uniform float uScrollOffset;\n  uniform vec2  uMouse;\n  uniform float uBevelSize;\n  uniform float uBevelStrength;\n  uniform float uFoilSaturation;\n  uniform float uFoilOpacity;\n  uniform float uFoilContrast;\n\n  varying vec2 vUv;\n  varying vec3 vWorldPosition;\n  varying vec3 vWorldNormal;\n\n  vec3 hueToRgb(float h) {\n    h = fract(h);\n    float r = abs(h * 6.0 - 3.0) - 1.0;\n    float g = 2.0 - abs(h * 6.0 - 2.0);\n    float b = 2.0 - abs(h * 6.0 - 4.0);\n    return clamp(vec3(r, g, b), 0.0, 1.0);\n  }\n\n  // Hard Light: foil acts as the light source painted over the certificate.\n  // blend = 0.5 → no change; < 0.5 → darkens (multiply); > 0.5 → brightens (screen).\n  vec3 hardLight(vec3 base, vec3 blend) {\n    vec3 dark   = 2.0 * base * blend;\n    vec3 bright = 1.0 - 2.0 * (1.0 - base) * (1.0 - blend);\n    return mix(dark, bright, step(0.5, blend));\n  }\n\n  void main() {\n    vec4 texColor = texture2D(map, vUv);\n    vec3 N = normalize(vWorldNormal);\n    vec3 V = normalize(uCameraPosition - vWorldPosition);\n    vec3 L1 = normalize(uLight1Position - vWorldPosition);\n    vec3 L2 = normalize(uLight2Position - vWorldPosition);\n\n    float NdotL1 = max(dot(N, L1), 0.0);\n    float NdotL2 = max(dot(N, L2), 0.0);\n    float diffuse = NdotL1 * uLight1Intensity + NdotL2 * uLight2Intensity;\n    diffuse = 0.5 + diffuse * 0.5;\n\n    float matte = 1.0 - matteRoughness * 0.3;\n    vec3 baseColor = texColor.rgb * matte * diffuse;\n\n    // ── Holographic foil ─────────────────────────────────────────────\n    float foilStrength = texture2D(uFoilMap, vUv).r;\n\n    float NdotV = max(dot(N, V), 0.0);\n    vec3  R     = reflect(-V, N);\n    float fresnel = pow(1.0 - NdotV, 2.5);\n\n    // ── Angle-dependent hue — NO atan, so no \xb1π wrap discontinuity ──\n    // Smooth dot-product projections of R onto two orthogonal reference\n    // directions; tilting the card rotates R and continuously shifts the hue.\n    float PI2     = 6.28318530718;\n    float viewHue = fract(R.x * 2.2 + R.y * 1.6 + R.z * 0.5 + uTime * 0.04\n                          + uScrollOffset + uMouse.x * 0.8 + uMouse.y * 0.5);\n\n    // ── Multi-frequency diffraction bands ──\n    // Integer coefficients → sin completes whole cycles over UV [0,1],\n    // so there is zero discontinuity at any UV boundary.\n    float g1 = sin((vUv.x *  7.0 + vUv.y *  3.0) * PI2);\n    float g2 = sin((vUv.x *  2.0 - vUv.y *  6.0) * PI2);\n    float g3 = sin((vUv.x * 10.0 + vUv.y *  7.0) * PI2);\n    float hue = fract(viewHue + g1 * 0.18 + g2 * 0.12 + g3 * 0.05 + fresnel * 0.08);\n\n    // White areas (foilStrength ≈ 1) → normal hue.\n    // Gray areas (foilStrength < 1 but > 0) → complementary hue (+0.5).\n    // smoothstep maps the transition so anti-aliased edges blend cleanly.\n    float isWhite = smoothstep(0.7, 0.97, foilStrength);\n    float effectiveHue = fract(hue + mix(0.5, 0.0, isWhite));\n\n    // Vivid, saturated rainbow\n    vec3 rainbow = hueToRgb(effectiveHue);\n    float luma = dot(rainbow, vec3(0.299, 0.587, 0.114));\n    rainbow = clamp(mix(vec3(luma), rainbow, uFoilSaturation), 0.0, 1.0);\n\n    // ── Foil stamp bevel: bump-map the edges of the foil mask ──\n    // Sample the mask at 4 neighbours to approximate the gradient.\n    // Non-zero gradient = edge of the stamp = raised bevel highlight/shadow.\n    vec2 foilGrad = vec2(\n      texture2D(uFoilMap, vUv + vec2( uBevelSize, 0.0)).r - texture2D(uFoilMap, vUv + vec2(-uBevelSize, 0.0)).r,\n      texture2D(uFoilMap, vUv + vec2(0.0,  uBevelSize)).r - texture2D(uFoilMap, vUv + vec2(0.0, -uBevelSize)).r\n    );\n    // Perturb the surface normal in UV/tangent space (PlaneGeometry: U→X, V→Y)\n    vec3 bumpN   = normalize(N + vec3(foilGrad.x, foilGrad.y, 0.0) * uBevelStrength);\n    float bumpD  = max(dot(bumpN, L1), 0.0) * uLight1Intensity\n                 + max(dot(bumpN, L2), 0.0) * uLight2Intensity;\n    float bumpDiffuse = 0.5 + bumpD * 0.5;\n    // Additive bevel: only the delta above normal diffuse, masked to stamp edge\n    float bevelMagnitude = length(foilGrad);\n    float bevel = (bumpDiffuse - diffuse) * bevelMagnitude * uBevelStrength * 1.2;\n    vec3 baseColor_beveled = baseColor + baseColor * bevel;\n\n    // ── Hard Light blend: foil tints the certificate without covering it ──\n    float metalDiff = mix(0.15, 1.0, pow(diffuse, 1.8));\n    float tintStr   = (0.38 + fresnel * 0.52) * metalDiff * uFoilContrast;\n    vec3  foilTint  = mix(vec3(0.5), rainbow, tintStr);\n\n    vec3 foilColor = hardLight(baseColor_beveled, foilTint);\n\n    // ── Additive specular on top (glint + sheen, no animated sparkle) ──\n    vec3  H1 = normalize(L1 + V);\n    vec3  H2 = normalize(L2 + V);\n    float sGlint = pow(max(dot(N, H1), 0.0), 256.0) * uLight1Intensity * 3.5\n                 + pow(max(dot(N, H2), 0.0), 256.0) * uLight2Intensity * 3.0;\n    float sSheen = pow(max(dot(N, H1), 0.0), 20.0)  * uLight1Intensity * 0.28\n                 + pow(max(dot(N, H2), 0.0), 20.0)  * uLight2Intensity * 0.22;\n    vec3 specColor = mix(rainbow, vec3(1.0), 0.6);\n    foilColor += specColor * sGlint + rainbow * sSheen * 0.45;\n\n    // ── Composite ──\n    // Use beveled base outside the foil area, foil overlay inside it\n    vec3 finalBase = mix(baseColor, baseColor_beveled, foilStrength);\n    vec3 composited = mix(finalBase, foilColor, foilStrength * uFoilOpacity);\n\n    // ── Shadow saturation: boost saturation in darker areas only ──\n    float compLuma = dot(composited, vec3(0.299, 0.587, 0.114));\n    float inShadow = 1.0 - smoothstep(0.0, 0.4, compLuma);\n    float shadowSatBoost = inShadow * 0.22;\n    composited = clamp(compLuma + (composited - compLuma) * (1.0 + shadowSatBoost), 0.0, 1.0);\n\n    // ── Film grain — value noise with bilinear interp to avoid stripes; stronger dark/light mix ──\n    const float GRAIN_FREQ = 3000.0;\n    vec2  grainCell  = floor(vUv * GRAIN_FREQ);\n    vec2  grainFrac  = fract(vUv * GRAIN_FREQ);\n    vec2  g       = grainFrac * grainFrac * (3.0 - 2.0 * grainFrac); // smoothstep for smooth blending\n    float h00     = fract(sin(dot(grainCell,              vec2(127.1, 311.7))) * 43758.5453);\n    float h10     = fract(sin(dot(grainCell + vec2(1,0),  vec2(269.5, 183.3))) * 43758.5453);\n    float h01     = fract(sin(dot(grainCell + vec2(0,1),  vec2(419.2, 371.9))) * 43758.5453);\n    float h11     = fract(sin(dot(grainCell + vec2(1,1),  vec2(83.7, 97.1))) * 43758.5453);\n    float grain   = mix(mix(h00, h10, g.x), mix(h01, h11, g.x), g.y);\n    float grainAdd = (grain - 0.5) * 0.14;\n    // In blacks: only add positive grain (compLuma from shadow-sat block) so dark areas get visible lighter speckles (avoid clamp-to-0)\n    float inDark = 1.0 - smoothstep(0.0, 0.1, compLuma);\n    grainAdd = mix(grainAdd, max(0.0, grainAdd), inDark);\n    composited += grainAdd;\n\n    gl_FragColor = vec4(composited, texColor.a);\n  }\n"},1762:(t,e,n)=>{n.d(e,{J3:()=>r,PR:()=>a,TN:()=>o,Uw:()=>u,gX:()=>s,hV:()=>c,te:()=>l,yX:()=>i});let o="usmen-marker-spawn",i="usmnt-marker-foil-cycle",a="[data-usmnt-dragon-foil]";function r(){if("undefined"==typeof document)return{clientX:0,clientY:0};let t=document.querySelector(a);if(t){let e=t.getBoundingClientRect();if(e.width>1&&e.height>1)return{clientX:e.left+.5*e.width,clientY:e.top+.5*e.height}}return{clientX:.5*window.innerWidth,clientY:.5*window.innerHeight}}function l(t){return t instanceof Element&&null!=t.closest(a)}function u(t,e){if("undefined"==typeof document)return!1;let n=document.querySelector(a);if(!n)return!1;let o=n.getBoundingClientRect();return!(o.width<=1)&&!(o.height<=1)&&t>=o.left&&t<=o.right&&e>=o.top&&e<=o.bottom}function s(t,e){window.dispatchEvent(new CustomEvent(o,{detail:{clientX:t,clientY:e}}))}function c(){window.dispatchEvent(new Event(i))}},2757:(t,e,n)=>{n.d(e,{k:()=>o});function o(){if("undefined"==typeof document)return 0;let t=getComputedStyle(document.documentElement).getPropertyValue("--initial-vh"),e=null==t?void 0:t.trim().match(/^([\d.]+)px$/);return e?parseFloat(e[1]):window.innerHeight}},5081:(t,e,n)=>{n.d(e,{Cu:()=>m,En:()=>v,GE:()=>l,JB:()=>d,MT:()=>u,Nt:()=>i,RN:()=>f,VI:()=>c,Yt:()=>h,d1:()=>p,d7:()=>a,mS:()=>g,td:()=>o,wy:()=>s});let o="#000000",i="#ae6808",a="#5197f7",r="usmnt-marker-yellow",l="usmnt-marker-yellow-change";function u(){return"undefined"!=typeof document&&document.documentElement.classList.contains(r)}function s(t){"undefined"!=typeof document&&u()!==t&&(document.documentElement.classList.toggle(r,t),window.dispatchEvent(new CustomEvent(l,{detail:{yellow:t}})))}let c=.1,m=.1,d=1.69;function f(){return"undefined"!=typeof document&&document.documentElement.classList.contains("site-inverted")}function v(){return u()}function h(){return v()&&!f()}function p(){return v()?a:g()}function g(){if(v())return i;if(!function(){if("undefined"==typeof document)return!1;let t=document.getElementById("aj1-main-footer");return!!t&&t.getBoundingClientRect().top<=2}())return o;let t=95+function(){if("undefined"==typeof document)return 0;let t=getComputedStyle(document.documentElement).getPropertyValue("--aj1-footer-hue-rotate").trim();if(!t)return 0;let e=parseFloat(t);return Number.isFinite(e)?e:0}();return"hsl(".concat(t," ").concat(97,"% ").concat(55,"%)")}},5310:(t,e,n)=>{n.d(e,{o:()=>a,s:()=>i});let o=!1;function i(t){o!==t&&(o=t,"undefined"!=typeof document&&(document.documentElement.classList.toggle("usmnt-marker-open",t),window.dispatchEvent(new Event("usmnt-marker-open-change"))))}function a(){return o}},6687:(t,e,n)=>{n.d(e,{U:()=>o});let o={curl:{amount:-1,tightness:1,origin:0,originEdge:0},scroll:{positionYStart:-.51,positionYStartMobile:-.25,positionYEnd:.64,positionYEndMobile:.29},animation:{scaleBase:.41,scaleTarget:1,scaleTargetAt:.22,startRotation:-45,startRotationAt:.16,exitStart:.68,exitEnd:1,exitScale:.58,exitRotation:16},light1:{intensity:1.14,position:[12.92,5,10]},light2:{intensity:.8,position:[8.34,5,10]},materials:{matteRoughness:.25,reflectiveStrength:.37,bevelSize:6e-4,bevelStrength:1.6,foilSaturation:.11,foilOpacity:.44,foilContrast:1.68},camera:{fov:20,position:[0,0,50]}}}}]);